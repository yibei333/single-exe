<Window x:Class="SingleExe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SingleExe"
        mc:Ignorable="d"
        Width="250"
        AllowsTransparency="True"
        SizeToContent="Height"
        WindowStyle="None"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="6">
            <TextBlock>正在准备文件,请稍候...</TextBlock>
            <ProgressBar Margin="0 6" 
                         Minimum="0" 
                         Maximum="100" 
                         Height="15"
                         Value="{Binding Progress}" />
            <TextBlock Foreground="Blue" Text="{Binding Filename}"/>
        </StackPanel>
    </Grid>
</Window>
